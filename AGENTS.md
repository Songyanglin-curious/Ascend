# AGENTS.md

## 目标

优先保证：边界正确、主线清晰、可验证、可归档。
不追求一次生成大量代码，不为“看起来完整”牺牲正确性。

## 语言

- 默认使用中文。
- `.md` 默认中文；代码注释默认中文；术语、标识符、接口字段保持原文，不强行翻译。
- 输出遵循：先结论，后细节；简洁直接；不重复，不铺垫，不空泛。

## 作用域

- 以当前操作文件所在目录向上查找，离得最近的 `AGENTS.md` 优先。
- 子目录规则覆盖父目录；无冲突规则叠加执行。

## 默认不读

以下内容默认不读取，除非 `00-task.md / 01-spec.md / 02-plan.md` 明确引用，或当前问题必须依赖：

- 笔记
- 素材
- Ascend演化流程

## 开工前必读

按顺序读取：

1. 当前生效的 `AGENTS.md`
2. `tasks/active/00-task.md`
3. `tasks/active/01-spec.md`
4. `tasks/active/02-plan.md`
5. `tasks/active/checklist.md`
6. 必要时再读 `docs/architecture/*.md`

## 任务定位

- 若无法确定当前任务，停止执行，只输出阻塞原因，不得猜测，不得直接开工。

## 阶段门禁

- 有 `00-task.md` 必先读。
- 没有 `01-spec.md`：只允许写 spec，不得写代码。
- 没有 `02-plan.md`：只允许写 plan，不得写代码。
- `02-plan.md` 不够细：先补 plan，不得写代码，不得生成 `checklist.md`。
- 只有 `02-plan.md` 达到可编码程度后，才允许生成或更新 `checklist.md`。
- 不得跳阶段。

## 文档冲突处理

- `00-task.md`、`01-spec.md`、`02-plan.md` 若冲突，停止编码，先修正文档。
- 不得自行脑补冲突含义后继续实现。

## 02-plan.md 最低要求

`02-plan.md` 必须细化到可以直接编码，至少明确：

- 改哪些文件 / 新增哪些文件
- 改哪个函数、类、模块
- 输入输出是什么
- 关键逻辑分支与异常路径
- 每一步验证命令
- 本次不做什么

## 执行边界

- 一次只实现一个里程碑。
- 只允许修改 `02-plan.md` 已列出的文件。
- 若需要改动计划外文件，先更新 `02-plan.md`，再继续实现。
- 未经允许，不做顺手重构，不扩大范围，不提前实现后续里程碑。

## 代码约束

- 代码必须突出主体逻辑和主路径。
- 禁止防御性编程淹没主逻辑。
- 能明确处理就处理；不能可靠处理就抛出。
- 禁止默认值掩盖问题。
- 禁止空 `catch`、静默跳过、伪 fallback。
- 新代码应职责单一、命名清楚、结构直接，像人写的代码。

## 输出契约

每次实际执行前，必须先明确：

- 当前阶段判断
- 本次要修改的文件
- 本次不修改的范围
- 验证命令
- 完成标准

## 测试与审查

- 实现后必须补测试；无法补测试时必须说明原因。
- 先自检改动与边界，再执行最小必要验证。
- 验证失败先修当前里程碑，不得带病进入下一步。

## 完成与归档

完成任务前必须：

- 更新 `checklist.md`
- 更新 `05-review.md`
- 如形成长期规则，反写 `AGENTS.md`
- 如改变全局设计，更新 `docs/architecture/*.md`

满足以上条件后，任务默认仍留在 `active/`。
只有在用户明确要求时，才允许移动到 `done/` 或 `archive/`。
