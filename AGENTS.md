# AGENTS.md

## 目标

优先保证：边界正确、主线清晰、可验证、可归档。
不为“看起来完整”牺牲正确性，不追求一次生成大量代码。

## 语言

默认使用中文。
`.md` 与代码注释默认中文；术语、标识符、接口字段保持原文。
输出遵循：先结论，后细节；简洁直接；不重复，不铺垫。

## 作用域

以当前操作目录向上查找，最近的 `AGENTS.md` 优先。
子目录规则覆盖父目录；无冲突规则叠加执行。

## 默认不读

以下内容默认不读取，除非当前任务明确引用或当前问题必须依赖：

- 笔记
- 素材
- Ascend 演化流程

## 开发原则

遵守 Spec-Driven Development，但默认采用轻流程。
除非用户明确声明进入重流程，否则一律按轻流程执行。
不得在边界不清时直接进入连续编码。

## 流程模式

### 轻流程（默认）

适用于：边界已基本明确、当前批次较小、目标是快速推进并控制跑偏。

规则：

- 默认只要求 `00-Requirement.md` 与 `01-spec.md`
- 默认只审核一次，且只审核 `01-spec.md`
- `01-spec.md` 审核通过后，可直接进入当前批次编码
- `02-plan.md`、`03-tasks.md`、`checklist.md`、`05-review.md` 在轻流程下不强制要求
- 若编码过程中发现 spec 不足以支撑继续实现，应立即停止编码，先补 spec，不得硬写
- 轻流程下仍不得跳过 requirement / spec 直接编码

### 重流程（需明确声明）

仅当用户明确要求“重流程”或当前任务明确要求严格门禁时，才进入重流程。

规则：

- 必须具备并审核：
  1. `00-Requirement.md`
  2. `01-spec.md`
  3. `02-plan.md`
  4. `03-tasks.md`
- 未完成上述审核前，不得进入编码
- 当前批次完成后，按要求更新 `checklist.md` 与 `05-review.md`

## 默认判定

若用户未明确说明流程模式，一律按轻流程执行。
AI 不得擅自升级为重流程；如判断当前任务风险较高，只能提示建议，不得自行切换流程模式。

## 开工规则

开工前按当前流程模式读取文档。

### 轻流程读取顺序

1. `AGENTS.md`
2. `tasks/active/00-Requirement.md`
3. `tasks/active/01-spec.md`

必要时再读：

- `tasks/active/02-plan.md`
- `docs/architecture/*.md`

### 重流程读取顺序

1. `AGENTS.md`
2. `tasks/active/00-Requirement.md`
3. `tasks/active/01-spec.md`
4. `tasks/active/02-plan.md`
5. `tasks/active/03-tasks.md`

必要时再读：

- `tasks/active/checklist.md`
- `docs/architecture/*.md`

若无法确定当前任务或文档存在关键缺失，停止执行，只输出阻塞原因，不得猜测。

## 文档读取原则

默认先读摘要，再决定是否下钻正文。
若文档包含摘要区，应优先依据摘要区判断当前任务边界、目标、阻塞与完成标准。
只有当摘要不足以支持当前任务时，才继续读取正文。

## 阶段门禁

### 轻流程

- 没有 `01-spec.md`：不得编码
- `01-spec.md` 未达到可编码程度：先补 spec，不得实现
- `00-Requirement.md` 与 `01-spec.md` 冲突：停止编码，先修正文档

### 重流程

- 没有 `01-spec.md`：只允许补 spec，不得写代码
- 没有 `02-plan.md`：只允许补 plan，不得写代码
- 没有 `03-tasks.md`：不得进入实现
- `00-Requirement.md`、`01-spec.md`、`02-plan.md`、`03-tasks.md` 冲突：停止编码，先修正文档

## 执行边界

一次只推进一个当前批次。
未经允许，不扩大范围，不顺手重构，不提前实现后续内容。
若发现当前任务已经超出当前批次边界，应停止实现，先回写 spec 或请求确认。

## 执行前说明

每次实际执行前，必须先明确：

- 当前流程模式（轻流程 / 重流程）
- 当前阶段判断
- 本次修改文件
- 本次不修改范围
- 验证命令
- 完成标准

## 代码原则

代码应突出主体逻辑和主路径。
能明确处理就处理；不能可靠处理就抛出。
禁止默认值掩盖问题，禁止空 `catch`、静默跳过、伪 fallback。
新代码应职责单一、命名清楚、结构直接。

## 持久化约束

当前持久化方案使用 SQLite（`better-sqlite3`）。
除非用户明确要求，不得擅自更换存储方案、引入 ORM、重构现有表结构。

db路径：`D:\db\sqlite\data\ascend.db`

当前树持久化以既有表结构为准：

- `nodes`
- `tree_state`
- `tree_relations`
- `child_candidate_events`

涉及节点、树关系、候选子节点落盘时，优先兼容现有表结构，不得平行再造另一套存储模型。

## 测试与验证

### 轻流程

- 先做最小必要验证
- 不强制补完整测试，但必须说明验证方式
- 若当前改动已具备明确测试入口，仍应补最小必要测试

### 重流程

- 实现后必须补最小必要测试
- 无法补测试时必须说明原因
- 先自检边界，再做最小必要验证

验证失败先修当前批次，不得带病进入下一步。

## 完成规则

### 轻流程

- 当前批次完成后，至少回写必要的 spec 变更或实现结论
- 不强制更新 `checklist.md` 与 `05-review.md`

### 重流程

- 完成任务前必须更新 `checklist.md` 与 `05-review.md`
- 如形成长期规则，再反写 `AGENTS.md`
- 未获明确要求，不得移动到 `done/` 或 `archive/`

## 输出要求

默认输出应便于直接审阅与决策。优先给出：

1. 当前判断
2. 修改范围
3. 风险 / 阻塞
4. 验证结果
5. 下一步建议

除非用户明确要求，不输出冗长过程性铺垫，不重复文档原文。
