# AGENTS.md

## 目标

本仓库使用 Codex 时，优先保证：边界正确、主线清晰、可验证、可归档，不追求一次生成大量代码。

## 语言

- 默认尽量使用中文。
- 先结论后细节，简洁直接。
- 不重复、不铺垫、不输出空泛解释。

## 开工前必读

按顺序读取：

1. 当前目录最近的 `AGENTS.md`
2. `tasks/active/<task-id>/00-task.md`
3. `tasks/active/<task-id>/01-spec.md`
4. `tasks/active/<task-id>/02-plan.md`
5. `tasks/active/<task-id>/checklist.md`
6. 必要时再读 `docs/architecture/*.md`

## 阶段门禁

- 没有 `01-spec.md`：先写 spec，不得写代码。
- 没有 `02-plan.md`：先写 plan，不得写代码。
- `02-plan.md` 不够细：先补 plan，不得写代码。
- 不得跳阶段。

## 02-plan.md 最低要求

必须明确：

- 改哪些文件 / 新增哪些文件
- 改哪个函数、类、模块
- 输入输出是什么
- 关键逻辑分支与异常路径
- 每一步验证命令
- 本次不做什么

## 实现方式

- 一次只实现一个里程碑。
- 未经允许，不做顺手重构，不扩大范围。
- 先小步可验证，再继续下一步。

## 架构硬边界

- Postgres/数据库是唯一业务真相库。
- Runtime/Checkpoint 只保存执行断面，不是真相。
- StepRecord 只在单轮完成后生成。
- Rescan 是同步领域动作，不弱化为随意后台任务。
- 前端只发命令、拉取 read model，不直接改业务真相。

## 代码约束

- 代码必须突出主体逻辑和主路径。
- 禁止防御性编程淹没主逻辑。
- 能明确处理就处理；不能可靠处理就抛出。
- 禁止用默认值、空 catch、静默跳过、伪 fallback 掩盖错误。
- 新代码应像人写的代码：职责单一、命名清楚、结构直接。

## 测试与审查

- 实现后必须补测试或说明为何当前无法补。
- 先审查再执行测试。
- 验证失败先修复当前里程碑，不得带病进入下一步。

## 完成与归档

完成任务前必须：

- 更新 `checklist.md`
- 更新 `05-review.md`
- 如形成长期规则，反写 `AGENTS.md`
- 如改变全局设计，更新 `docs/architecture/*.md`
- 满足以上条件后，才允许从 `active/` 移到 `done/` 或 `archive/`
