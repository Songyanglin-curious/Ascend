# AGENTS.md

## 1. 目标

本仓库使用 Codex 时，必须以“**正确推进项目**”为第一目标，而不是以“尽快生成代码”或“多写一点看起来完整的代码”为目标。

所有工作都必须服从以下原则：

1. 先理解边界，再设计，再实现。
2. 先形成文件化约束，再进行代码生成。
3. 一次只推进一个清晰的任务单元。
4. 不得跳阶段。
5. 数据库是真相库，运行时不是。
6. 主体逻辑必须清晰，禁止防御性编程淹没主线。

---

## 2. 语言与输出风格

1. 默认尽量使用中文输出，除非任务明确要求英文。
2. 输出优先清晰、简洁、可执行，不要堆砌空话。
3. 先给结论，再给细节。
4. 不要为了显得稳妥而输出大量模糊提醒。
5. 如果不确定，明确指出不确定点，不要假装已经解决。

---

## 3. 仓库工作方式

你必须把本仓库视为“**文件驱动的任务推进系统**”，而不是一段临时聊天。

所有正式推进都围绕以下文件组织：

- `docs/architecture/`
- `docs/standards/`
- `tasks/active/<task-id>/`
- `tasks/done/`
- `tasks/archive/`

如果任务发生在某个 `tasks/active/<task-id>/` 目录下，则该目录中的文件优先于聊天中的模糊描述。

---

## 4. 关键文件职责

### 4.1 全局架构文件

位于：

- `docs/architecture/ascend-1.0-design.md`
- `docs/architecture/ascend-1.0-dev-design.md`

职责：

- 定义全局架构
- 定义核心对象
- 定义模块边界
- 定义主链和约束

这些文件是系统级背景，不是当前任务单。

### 4.2 当前任务文件

位于：

- `tasks/active/<task-id>/00-task.md`
- `tasks/active/<task-id>/01-spec.md`
- `tasks/active/<task-id>/02-plan.md`
- `tasks/active/<task-id>/checklist.md`
- `tasks/active/<task-id>/05-review.md`

职责：

#### `00-task.md`

定义当前问题、当前边界、当前不做什么。

#### `01-spec.md`

定义当前任务在全局架构下的局部规格。

#### `02-plan.md`

定义当前任务的执行计划。
注意：`02-plan.md` 必须细到可直接施工，不允许只有高层口号。

#### `checklist.md`

定义验收条件、测试条件、审查条件。

#### `05-review.md`

记录完成结果、偏差、遗留问题、后续建议。

---

## 5. 阶段门禁规则

任何任务都必须严格按以下顺序推进：

1. `00-task.md`
2. `01-spec.md`
3. `02-plan.md`
4. 正式代码与测试
5. 测试审查
6. `05-review.md`
7. 归档

### 5.1 没有 spec 时

如果当前任务目录下没有 `01-spec.md`，必须先生成 spec。
不得直接写代码。

### 5.2 没有 plan 时

如果当前任务目录下没有 `02-plan.md`，必须先生成 plan。
不得直接写代码。

### 5.3 plan 不够细时

如果 `02-plan.md` 没有细到“可直接施工”的粒度，必须先补 plan。
不得直接写代码。

### 5.4 没有 checklist 时

如果当前任务缺少 `checklist.md`，必须先补 checklist。
不得直接进入最终验收或归档。

### 5.5 不得跳阶段

未经明确允许，不得绕过 spec / plan / checklist 直接生成大量代码。
不得在 plan 仍模糊时开始实现。

---

## 6. 02-plan.md 的最低要求

`02-plan.md` 必须至少写清以下内容：

### 6.1 目标

- 这次要完成什么
- 明确不完成什么

### 6.2 影响范围

- 改哪些文件
- 新增哪些文件
- 哪些文件禁止修改

### 6.3 模块 / 函数改动表

明确到：

- 文件路径
- 函数 / 类 / 模块名
- 这次为什么改它
- 这次改它做什么

### 6.4 输入输出

对关键改动写清：

- 输入是什么
- 输出是什么
- 状态变化是什么
- 谁调用谁

### 6.5 关键逻辑分支

必须写清：

- 正常路径
- 异常路径
- 边界条件
- 是否触发等待人工
- 是否触发 Rescan
- 是否发生冻结 / 重开 / 完成

### 6.6 实现顺序

按里程碑列出：

- 第一步改什么
- 第二步改什么
- 每一步如何验证

### 6.7 验证命令

写清：

- 测试命令
- 类型检查命令
- lint 命令
- 其他必要验证方式

如果 `02-plan.md` 不满足上述要求，则视为 **不能施工**。

---

## 7. 实现推进规则

### 7.1 一次只做一个里程碑

不要一口气实现整个计划。
必须按 `02-plan.md` 的里程碑逐步推进。

### 7.2 每次实现必须说明范围

在真正改代码前，先明确：

- 本次只实现哪一个里程碑
- 本次不会碰哪些部分

### 7.3 优先小步可验证

优先做：

- 范围小
- 可验证
- 容易回滚
- 不跨太多层

的实现。

### 7.4 不要顺手重构

如果 spec / plan 没要求，不要顺手做大规模重构。
不要把当前任务扩展成“顺便整理整个项目”。

---

## 8. 代码实现约束

### 8.1 主体逻辑优先

代码必须突出主路径和主体逻辑。
不要让大量边界校验、默认值、fallback、空分支把主逻辑淹没。

### 8.2 禁止防御性编程泛滥

除非 spec / plan 明确要求，否则不要主动加入大量 defensive programming。

禁止以下行为：

- 随意给默认值掩盖错误
- 用空 catch 吞异常
- 遇到异常直接跳过
- 返回看似成功的伪结果
- 用大量无意义 if/else 保护主路径
- 为了避免报错而机械性添加 `|| 默认值`、`?? 默认值`

### 8.3 异常处理原则

遇到异常时：

- 能明确处理，就明确处理
- 不能可靠处理，就允许抛出
- 不要伪造稳定性
- 不要为了“程序不断”而牺牲正确性

### 8.4 可读性要求

新代码应尽量像人写的代码：

- 主线清晰
- 函数职责单一
- 输入输出直接
- 命名明确
- 结构稳定

### 8.5 质量判断

如果出现以下情况，视为质量下降：

- 校验代码明显多于主体逻辑
- fallback 明显多于真实处理
- 异常被掩盖而不是被暴露
- 代码看完仍看不出主路径
- 生成了很多边角保护，但核心实现很弱

出现这类情况时，应主动收缩、重写或简化。

---

## 9. 架构边界约束

### 9.1 数据库是真相库

所有正式状态、正式留痕、正式关系，都必须以数据库为业务真相。

### 9.2 Runtime 不是业务真相

运行时只承载：

- 断面
- checkpoint
- currentMode
- waitHuman
- 中间过程

不得把 Runtime 当正式状态来源。

### 9.3 StepRecord 生成时机

StepRecord 只能在单轮完成后生成。
不得在中间过程提前生成正式留痕。

### 9.4 Rescan 规则

Rescan 是同步领域动作。
它必须在主链落账语义内完成，不应被当作随意后台化的弱动作。

### 9.5 前端边界

前端只能：

- 拉取 read model
- 展示图与详情
- 发命令

前端不得直接修改业务真相层级、正式状态和正式关系。

---

## 10. 测试与验证规则

### 10.1 代码生成后必须补测试

如果实现会影响逻辑正确性，必须补测试或明确说明为什么当前无法补。

### 10.2 测试前先审查

生成测试后，不要直接假设通过。必须先按 checklist 审查：

- 覆盖主路径没有
- 覆盖关键分支没有
- 覆盖异常路径没有
- 有没有只测表面不测逻辑

### 10.3 验证失败处理

如果验证失败：

1. 先修复当前里程碑
2. 不得带着失败进入下一个里程碑
3. 不得通过降低标准来强行宣布完成

---

## 11. Review 规则

任务完成后，必须更新 `05-review.md`。至少写清：

- 实际改了什么
- 哪些点与 plan 有偏差
- 哪些地方存在遗留问题
- 哪些经验应反写到全局规则

如果这次任务暴露出新的长期规则，应更新 `AGENTS.md`。
如果这次任务改变了全局架构，应更新 `docs/architecture/*.md`。

---

## 12. 归档规则

### 12.1 何时可以归档

只有在以下条件全部满足时，才允许从 `active/` 移出：

- spec 完整
- plan 完整
- checklist 已核对
- review 已完成
- 测试与审查已完成

### 12.2 归档路径

- 刚完成先进入 `tasks/done/`
- 冷却后再进入 `tasks/archive/`

### 12.3 不得直接跳归档

不得在没有 review 的情况下直接归档。
不得在 checklist 未完成的情况下直接归档。

---

## 13. 线程与任务规则

1. 一个线程尽量只推进一个清晰任务单元。
2. 同一问题的 spec / plan / code / test / review 尽量留在同一线程。
3. 只有在真正并行、且互不踩文件时，才允许显式请求 subagent。
4. 不得默认开启多 agent 并行。

---

## 14. 需要优先阅读的文件

开始任何实际工作前，优先阅读：

1. `AGENTS.md`
2. 当前任务目录的 `00-task.md`
3. 当前任务目录的 `01-spec.md`
4. 当前任务目录的 `02-plan.md`
5. 当前任务目录的 `checklist.md`
6. 必要时再读 `docs/architecture/*.md`

如果这些文件缺失或冲突，先指出问题并停止进入编码。

---

## 15. 最终要求

你不是来“尽量生成更多代码”的。你是来：

- 按文件化约束推进
- 保住边界
- 保住主线
- 生成清晰的人类可接手代码
- 让结果可审查、可验证、可归档

如果当前信息不足以进入编码，应先补 spec / plan / checklist，**而不是硬写代码**。
